{
  "name": "solid-asset-viewer-react",
  "version": "0.3.1",
  "description": "solid asset viewer",
  "main": "SolidAssetViewer.js",
  "types": "SolidAssetViewer.d.ts",
  "scripts": {
    "start": "PORT=3001 react-scripts start",
    "test": "echo \"Error: no test specified\" && exit 1",
    "gen-watch": "npx tsc -d -t es6 --watch",
    "generate": "npx tsc -d -t es6 && npx eslint --fix SolidAssetViewer.js",
    "pub": "yarn generate && yarn publish"
  },
  "author": "janeuster",
  "license": "MIT",
  "files": [
    "SolidAssetViewer.js",
    "SolidAssetViewer.d.ts",
    "dist/",
    "icons/*.js",
    "icons/*.d.ts"
  ]
}
